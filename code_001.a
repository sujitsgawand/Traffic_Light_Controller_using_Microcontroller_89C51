ORG 0000H
SJMP MAIN
MAIN:MOV SP,#50H
	 MOV R0,#5H
	 MOV P0,#01H
	 MOV P3,#24H
	 MOV P1,#21H
	 ACALL UP
	  
     MOV P0,#02H
	 MOV P3,#24H
	 MOV P1,#0CH
	 ACALL UP

	 MOV P0,#04H
	 MOV P3,#21H
	 MOV P1,#24H
	 ACALL UP

	 MOV P0,#08H
	 MOV P3,#0CH
	 MOV P1,#24H
	 ACALL UP
     SJMP MAIN

UP: MOV P2,#90H
	ACALL DELAY
	MOV P2,#80H
	ACALL DELAY
	MOV P2,#0F8H
	ACALL DELAY
	MOV P2,#82H
	ACALL DELAY
	MOV P2,#92H
	ACALL DELAY
	DJNZ R0,NEXT
	SJMP MAIN
